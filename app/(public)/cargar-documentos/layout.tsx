// Título de la pestaña: la página es client component y no puede exportar metadata.
export const metadata = { title: 'Carga de documentos — Cofianza' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
